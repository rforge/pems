#sources


ref.list <- ref.unit.conversions 

#update ref.list
#the changes
#note overwrite

ref.list <- addUnitConversion(to = "km/h", from = "mi/h", conversion = function(x) x * 1.609344, 
                      tag = "miles/hour to kilometers/hour", unit.conversions = ref.list, overwrite=TRUE)

ref.list <- addUnitConversion(to = "mi/h", from = "km/h", conversion = function(x) x * 0.62137119224, 
                      tag = "kilometers/hour to miles/hour", unit.conversions = ref.list, overwrite=TRUE) 

ref.list <- addUnitConversion(to = "m/s", from = "km/h", conversion = function(x) x * 0.27777777778, 
                      tag = "kilometers/hour to meters/second", unit.conversions = ref.list, overwrite=TRUE) 

ref.list <- addUnitConversion(to = "mi/h", from = "km/h", conversion = function(x) x * 0.44704, 
                      tag = "miles/hour to meters/second", unit.conversions = ref.list, overwrite=TRUE) 

ref.list <- addUnitConversion(to = "km/h", from = "m/s", conversion = function(x) x * 3.6, 
                      tag = "meters/second to kilometers/hour", unit.conversions = ref.list, overwrite=TRUE) 

ref.list <- addUnitConversion(to = "mi/h", from = "m/s", conversion = function(x) x * 2.2369362921, 
                      tag = "meters/second to miles/hour", unit.conversions = ref.list, overwrite=TRUE) 

ref.list <- addUnitConversion(to = "knots", from = "m/s", conversion = function(x) x * 1.9438444925, 
                      tag = "meters/second to knots", unit.conversions = ref.list, overwrite=TRUE) 

ref.list <- addUnitConversion(to = "knots", from = "mi/h", conversion = function(x) x *  0.86897624191, 
                      tag = "miles/hour to knots", unit.conversions = ref.list, overwrite=TRUE)

ref.list <- addUnitConversion(to = "knots", from = "km/h", conversion = function(x) x *  0.53995680346, 
                      tag = "kilometers/hour to knots", unit.conversions = ref.list, overwrite=TRUE)

ref.list <- addUnitConversion(to = "m/s", from = "knots", conversion = function(x) x * 0.51444444444, 
                      tag = "knots to meters/second", unit.conversions = ref.list, overwrite=TRUE)

ref.list <- addUnitConversion(to = "mi/h", from = "knots", conversion = function(x) x * 1.150779448, 
                      tag = "knots to miles/hour", unit.conversions = ref.list, overwrite=TRUE)
 
ref.list <- addUnitConversion(to = "km/h", from = "knots", conversion = function(x) x * 1.852, 
                      tag = "knots to kilometers/hour", unit.conversions = ref.list, overwrite=TRUE)

ref.list <- addUnitConversion(to = "m/s/s", from = "km/h/s", conversion = function(x) x * 0.27777777778, 
                      tag = "kilometers/hour second to meters/square second", unit.conversions = ref.list, overwrite=TRUE) 

ref.list <- addUnitConversion(to = "m/s/s", from = "mi/h/s", conversion = function(x) x * 0.44704, 
                      tag = "miles/hour second to meters/square second", unit.conversions = ref.list, overwrite=TRUE)

ref.list <- addUnitConversion(to = "km/h/s", from = "mi/h/s", conversion = function(x) x * 1.609344, 
                      tag = "miles/hour second to kilometers/hour second", unit.conversions = ref.list, overwrite=TRUE)

ref.list <- addUnitConversion(to = "mi/h/s", from = "km/h/s", conversion = function(x) x * 0.62137119224, 
                      tag = "kilometers/hour second to miles/hour second", unit.conversions = ref.list, overwrite=TRUE) 

ref.list <- addUnitConversion(to = "km/h/s", from = "m/s/s", conversion = function(x) x * 3.6, 
                      tag = "meters/square second to kilometers/hour second", unit.conversions = ref.list, overwrite=TRUE) 

ref.list <- addUnitConversion(to = "mi/h/s", from = "m/s/s", conversion = function(x) x * 2.2369362921, 
                      tag = "meters/square second to miles/hour second", unit.conversions = ref.list, overwrite=TRUE) 

ref.list <- addUnitConversion(to = "K", from = "dgrC", conversion = function(x) x + 273.15, 
                      tag = "degrees Celsius to Kelvin", unit.conversions = ref.list, overwrite=TRUE) 

ref.list <- addUnitConversion(to = "dgrC", from = "K", conversion = function(x) x - 273.15, 
                      tag = "Kelvin to degrees Celsius", unit.conversions = ref.list, overwrite=TRUE)

ref.list <- addUnitConversion(to = "dgrF", from = "dgrC", conversion = function(x) (9/5) * 1 + 32, 
                      tag = "degrees Celsius to degrees Farenheit", unit.conversions = ref.list, overwrite=TRUE) 

ref.list <- addUnitConversion(to = "dgrC", from = "dgrF", conversion = function(x) (x - 32) * (5/9), 
                      tag = "degrees Farenheit to degrees Celsius", unit.conversions = ref.list, overwrite=TRUE) 

ref.list <- addUnitConversion(to = "s", from = "min", conversion = function(x) x * 60, 
                      tag = "minutes to seconds", unit.conversions = ref.list, overwrite=TRUE)

ref.list <- addUnitConversion(to = "s", from = "h", conversion = function(x) x * 3660, 
                      tag = "hours to seconds", unit.conversions = ref.list, overwrite=TRUE)

ref.list <- addUnitConversion(to = "min", from = "h", conversion = function(x) x * 60, 
                      tag = "hours to minutes", unit.conversions = ref.list, overwrite=TRUE)

ref.list <- addUnitConversion(to = "min", from = "s", conversion = function(x) x / 60, 
                      tag = "seconds to minutes", unit.conversions = ref.list, overwrite=TRUE)

ref.list <- addUnitConversion(to = "h", from = "s", conversion = function(x) x / 3660, 
                      tag = "seconds to hours", unit.conversions = ref.list, overwrite=TRUE)

ref.list <- addUnitConversion(to = "h", from = "min", conversion = function(x) x / 60, 
                      tag = "minutes to hours", unit.conversions = ref.list, overwrite=TRUE)

ref.list <- addUnitConversion(to = "m", from = "km", conversion = function(x) x * 1000, 
                      tag = "kilometers to meters", unit.conversions = ref.list, overwrite=TRUE)

ref.list <- addUnitConversion(to = "km", from = "m", conversion = function(x) x / 1000, 
                      tag = "meters to kilometers", unit.conversions = ref.list, overwrite=TRUE)

ref.list <- addUnitConversion(to = "km", from = "mi", conversion = function(x) x * 1.609344, 
                      tag = "miles (int) to kilometers", unit.conversions = ref.list, overwrite=TRUE)

ref.list <- addUnitConversion(to = "mi", from = "km", conversion = function(x) x * 0.621371192, 
                      tag = "kilometers to miles (int)", unit.conversions = ref.list, overwrite=TRUE)

ref.list <- addUnitConversion(to = "m", from = "mi", conversion = function(x) x * 1609.344, 
                      tag = "miles (int) to meters", unit.conversions = ref.list, overwrite=TRUE)

ref.list <- addUnitConversion(to = "mi", from = "m", conversion = function(x) x * 0.000621371, 
                      tag = "meters to miles (int)", unit.conversions = ref.list, overwrite=TRUE)


#add alias
#needs to be re done if add new to or from

ref.list <- addUnitAlias("km/h", "kph", ref.list)
ref.list <- addUnitAlias("mi/h", "mph", ref.list)

#check list
ref.list

#wipe
rm(list=ls()[ls() != "ref.list"])
ref.unit.conversions <- ref.list

rm(ref.list)


#archive the old conversion
#reset the R directory

#then save the new ref file

#save(ref.unit.conversions, file="ref.unit.conversions.rda")


#save workspace as ref.RData
#and copy to pems.utils data directory

